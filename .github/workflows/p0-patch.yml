name: p0-patch
on: push
permissions:
  contents: write
jobs:
  patch:
    if: ${{ contains(github.event.head_commit.message, 'p0-run') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/p0_patch.py
      - run: |
          git config user.name "pjkuo"
          git config user.email "kuo0615@gmail.com"
          git rm -q .github/workflows/p0-patch.yml .github/p0_patch.py
          git add -A
          git commit -m "P0 入口頁精簡：新增『今天要做什麼』4 大鈕（含我的學習週報）；平台／工具／影片區收進摺疊；閘門與 AE Router 未動" -m "Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_0132fkQFFpgYs7gD13EjefU8"
          git push
